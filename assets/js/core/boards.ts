import type { DiagnosticsPort } from '../../../src/platform/contracts/diagnostics.ts';import type { QueryClient } from '../../../src/platform/contracts/query.ts';import type { AuthTransportPort } from '../../../src/platform/contracts/transport.ts';import type { BoardDomainService } from '../../../src/features/boards/contracts/service.ts';import { createBoardRepository } from '../features/boards/data/board-repository.ts';import { createBoardDomainService } from '../features/boards/services/board-domain-service.ts';
export interface BoardServiceFactoryOptions{readonly queryClient?:QueryClient|null;readonly diagnostics?:DiagnosticsPort|null;}
export function createBoardService(auth:AuthTransportPort,options:BoardServiceFactoryOptions={}):BoardDomainService{return createBoardDomainService({repository:createBoardRepository(auth,options)});}
export {createBoardRepository} from '../features/boards/data/board-repository.ts';export {createBoardDomainService} from '../features/boards/services/board-domain-service.ts';
