import type { RouteAccessContext, RouteAccessDecision, RoutePolicyService } from '../../../../src/platform/contracts/routing.ts';
const ANON=new Set(['login','register','verify'] as const);const ENTRY=new Set(['login','register'] as const);
export function createRoutePolicyService():RoutePolicyService{return Object.freeze({decide(context:RouteAccessContext):RouteAccessDecision{if(!context.initialized||context.status==='initializing'||context.status==='restoring')return Object.freeze({kind:'wait'});if(context.status==='disabled')return Object.freeze({kind:'render-disabled'});if(!context.authenticated&&!ANON.has(context.route.name as 'login'|'register'|'verify'))return Object.freeze({kind:'redirect',target:'login',rememberReturnRoute:true});if(context.authenticated&&ENTRY.has(context.route.name as 'login'|'register'))return Object.freeze({kind:'redirect',target:'',rememberReturnRoute:false});return Object.freeze({kind:'allow'});}} satisfies RoutePolicyService);}
