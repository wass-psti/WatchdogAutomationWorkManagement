import type { PlatformServiceOptions, PlatformServices } from '../../../src/platform/contracts/composition.ts';
import { applicationManifest, validateApplicationManifest } from '../../../config/application-manifest.ts';import { createDiagnostics } from '../platform/observability/diagnostics.ts';import { createQueryClient } from '../platform/data/query-client.ts';import { createBackendClient } from '../platform/data/backend-client.ts';import { normalizeAppError } from '../platform/errors/app-error.ts';import { hasPlatformCapability } from '../platform/auth/permissions.ts';import { createBoardRepository } from '../features/boards/data/board-repository.ts';import { createBoardDomainService } from '../features/boards/services/board-domain-service.ts';import { createBoardCommandService } from '../features/boards/services/board-command-service.ts';import { createRoutePolicyService } from './services/route-policy.ts';
export function createPlatformServices(options:PlatformServiceOptions):PlatformServices{const auth=options.auth;const diagnostics=createDiagnostics({limit:options.diagnosticLimit??160});const serverState=createQueryClient({diagnostics,defaultStaleTime:options.queryStaleTime??10_000});const backend=createBackendClient(auth,{diagnostics});const repository=createBoardRepository(auth,{queryClient:serverState,backendClient:backend,diagnostics});const service=createBoardDomainService({repository});const commands=createBoardCommandService({service});const routing=createRoutePolicyService();const manifest=Object.freeze({value:applicationManifest,validate:()=>validateApplicationManifest(applicationManifest)});const authorization=Object.freeze({hasPlatformCapability});const errors=Object.freeze({normalize:normalizeAppError});const boards=Object.freeze({repository,service,commands});return Object.freeze({auth,diagnostics,serverState,backend,boards,routing,manifest,authorization,errors} satisfies PlatformServices);}
